# ( Sway + Alacritty + Waybar ) config files

It is recommended that you have the following fonts installed:

- SourceCodePro (`adobe-source-code-pro-fonts`)
- Roboto Mono (`ttf-roboto-mono`)
- Font Awesome (`ttf-font-awesome`)

